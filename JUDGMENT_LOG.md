# 判断記録 (Judgment Log)

このドキュメントは、PdM (筒井) が AI コーディングアシスタント (Claude Code) と並走しながら本リポの仕様策定・実装を進めた際に、**「AI が出した初期案」と「PdM が rollback または修正して採用した最終案」の差分とその理由** を時系列で記録するものです。

## なぜこの記録を残すのか

AI 補助環境では output 量が増える反面、**「どこが PdM の判断で、どこが AI 由来か」が外部から見えにくくなる** 課題があります。本記録はその透明性を確保し:

- PdM 固有の付加価値 (ドメイン知識・運用判断・UX 直感) を可視化
- 同じ判断軸を別の案件でも再現できるよう参照可能化
- 採用しなかった A 案も残し、「なぜ B が良かったか」の根拠を後から検証可能に

を目的としています。

## 運用ルール

1. **仕様変更時**: Claude が初期案を出し、PdM が rollback / 修正した場合は本表に追記
2. **commit message**: 「`判断記録: rollback判断: A (旧) → B (採用), 理由: ...`」のフォーマットを採用
3. **A 案でも動いたケース**: 「結果論ではなく判定基準の根拠」を残すために A 案も記録
4. **半年に 1 回**: 本表をレビューし、本リポ固有の判断軸を末尾セクションに抽出

---

## 判断インデックス (本リポでの判断一覧)

> 個別の詳細はこの下の `## 判断詳細` 各セクションを参照

| ID | 日付 | テーマ | Claude 案 (要約) | PdM 採用案 (要約) |
|---|---|---|---|---|
| J-001 | 2026-05-29 | 月セレクタの年度グルーピング | モックで `【2026年度】…|【2025年度】…` の年度見出しグルーピングを提案 | **不採用**。YYYYMM絶対値の降順フラットリストで確定 |
| J-002 | 2026-05-29 | 予算開始月のデフォルト | （実装は4月）／Claudeは後方互換から1月を推奨 | **1月（暦年）で確定**。default=4はバックフィルで既存全テナントが事故 |

---

## 判断詳細

### J-001 — 2026-05-29 — 月セレクタの年度グルーピング

**背景**:
- 予算開始月設定の影響で、月次集計・進捗分析・感度分析の単月セレクタの選択肢をどう並べるか。仕様モックでは年度見出しグルーピングを提案、実装は絶対YYYYMM降順フラットだった。

**Claude 提案 (A 案)**:
- 月セレクタを `【2026年度】2026年04月…2027年03月 | 【2025年度】…` と会計年度見出しでグルーピングする。

**PdM 採用 (B 案)**:
- **不採用**。実装どおり YYYYMM 絶対値の降順フラットリスト（過去3会計年度〜当月）で確定。

**採用理由**:
- **主たる理由: 仮にFY所属を示す必要が出ても、最適解は見出しグルーピングではなく「FYサフィックス方式」だから。** よって見出しは採らず、表示は現状のフラット降順のまま。
- FYサフィックス方式（各月に `(FY2025)`／FY境界に細い区切り線）は縦に伸びず直近月スキャンの速さを損なわない。`budget_start_month=1`（大多数）では無情報なので非表示、`≠1` のときだけ出せばよく現状のフラットを土台に最小追加で済む。
- 見出しグルーピングはこの軽量方式より重く、単月選択ではFY所属が表示データに影響しないため割に合わない。
- 範囲ピッカー（年間予実）は別物で、年度跨ぎ選択ニーズが出れば年度プリセットが適切。

**判断軸への寄与**: 「冗長を削る（重い見出しより軽量サフィックス）」「責務境界（単月ピッカー vs 範囲ピッカーで設計を分ける）」。

---

### J-002 — 2026-05-29 — 予算開始月のDBデフォルト

**背景**:
- `companies.budget_start_month` 追加にあたり、デフォルト値を何月にするか。エンジニア実装は暫定で4月、仕様は `default: 1`。

**Claude 提案 / 推奨**:
- `default: 1`（1月＝暦年）を推奨。

**PdM 採用**:
- **1月で確定**。

**採用理由**:
- 現状の全画面は暦年基準。`default: 1` なら既存全企業の表示が一切変わらない（後方互換）。
- `default: 4` 等にすると `add_column` のバックフィルで既存全行が4月始まりになり、誰も設定変更していないのにデプロイ直後に全テナントの予算画面が一斉にずれる＝事故。
- `year` パラメータ（費用設定:履歴）の意味変更（暦年→会計年度開始年）も、`default: 1` なら指す範囲が実質同一で既存URL/ブックマークが壊れない。
- 4月会計年度の企業は会社情報画面で各自オプトイン。

**判断軸への寄与**: 「移行時にユーザー体験を壊さない（後方互換・オプトイン）」。

---

## 本リポで一貫して優先した判断軸 (継続更新)

本リポでの判断記録が積み重なってきたら、以下のような形で **再現可能な原則** として抽出していきます。半年〜1 年ごとに見直し:

例:
- 判別不可ケースで UI が破綻しない設計を優先
- 責務境界 (どこからどこまでがこの機能の領分か) の明確化
- 冗長を削る (UI 表記、説明文、機能オプション)
- ブラウザネイティブ UI を排除しカスタム UI に統一
- 明示的なユーザーアクション (自動遷移より「次へ」明示クリック)
- 状態管理の排他制御 (checkbox より radio をブラウザ強制で活用)
- AI の「自分の構造に従いがち」傾向を見抜くメタ判断

---

## 関連リソース

- **全リポ共通の運用方針 / 判断軸 (大規模事例)**: [rakmy-owner-registration-mock §23.20.17](https://github.com/suguru789987/rakmy-owner-registration-mock/blob/main/20260501_新規登録運用登録分離仕様.md)
- **詳細記録の参考実装**: [rakmy-timerecord-import-spec JUDGMENT_LOG.md](https://github.com/suguru789987/rakmy-timerecord-import-spec/blob/main/JUDGMENT_LOG.md)
- **ポートフォリオ**: [suguru789987](https://github.com/suguru789987/suguru789987)
